-- Session 切分:同一用户相邻事件间隔 > 30 分钟视为新会话

-- 1) 给每条行为打 session_seq:LAG 求间隔 → 标记新会话 0/1 → 累计求和

-- 2) 按 session 聚合:行为数、时长(秒)、pv 数、buy 数

-- 3) 找出 buy > 0 且 pv = 0 的 session(有买没看)

-- 4) 总会话数、人均会话数、平均会话时长
